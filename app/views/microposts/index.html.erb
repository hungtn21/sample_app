<h1><%= t("microposts.index.title") %></h1>

<div id="microposts">
  <% @microposts.each do |micropost| %>
    <%= render micropost %>
    <p>
      <%= link_to t(".show_link"), micropost %>
    </p>
  <% end %>
</div>

<%= link_to t(".new_link"), new_micropost_path %>
